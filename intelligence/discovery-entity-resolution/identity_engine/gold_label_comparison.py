# Compare against gold labels
# Compare resolved identities against gold standard labels.

def compare_to_gold_labels(predicted_clusters, gold_labels):
    """Compare predicted clusters to gold labels.
    Placeholder: return dummy metrics.
    """
    return {
        'precision': 0.0,
        'recall': 0.0,
        'f1': 0.0
    }

def load_gold_labels(path):
    """Load gold labels from file."""
    # Placeholder
    return []