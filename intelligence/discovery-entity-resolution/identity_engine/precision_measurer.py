# Measure precision
# Measure precision of identity resolution.

def measure_precision(predicted_clusters, gold_labels):
    """Measure precision of predicted clusters against gold labels.
    Placeholder: return 0.0.
    """
    return 0.0

def measure_recall(predicted_clusters, gold_labels):
    """Measure recall of predicted clusters against gold labels.
    Placeholder: return 0.0.
    """
    return 0.0