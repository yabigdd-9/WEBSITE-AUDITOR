"""Dashboard upgrades."""
UPGRADES = [
    "1. Executive Summary card (plain English, AI-generated)",
    "2. Category scorecards (Security/SEO/Performance/Accessibility)",
    "3. Evidence viewer (click defect → see raw HTTP headers/DOM snippet)",
    "4. Before/After trend chart (when multiple audits exist)",
    "5. White-label branding (agency logo, colours, client name)",
    "6. PDF export (via browser print → Save as PDF)",
    "7. Quick-Wins section (fixes under 15 minutes)",
    "8. Client approval buttons (approve → triggers Action Engine)",
    "9. Dark/light mode toggle",
    "10. Mobile-responsive layout",
]
for u in UPGRADES:
    print(f"  {u}")
