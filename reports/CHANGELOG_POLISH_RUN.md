# WEBSITES/BUISNESSaudits — polish change log

The exact source-file manifest and binary-capable Git patch are supplied alongside these reports. The original source/Git state and consistent SQLite snapshot were backed up before repair. 239 regular source-file hashes were verified in a disposable archive restore; Git also preserves original symlink targets. The three pre-existing outreach edits were copied and hash-checked separately.

Changed behavior: relative compatibility paths restore the flattened source layout; root mm and Desktop resolve one current runtime; current labels use the canonical name; the email qualifier's missing normalizer/budget return are repaired without scoring-threshold changes; real historical fixtures restore two regressions; Hermes capability detection uses the installed interface and blocks unapproved inference; legacy SMTP/Gmail paths stop before credentials; network uncertainty no longer becomes a fabricated website defect; fresh validation is bounded/frozen/file-only; audit packets require visible evidence and stop at human approval.

Meaningful correction during validation: the browser found hidden optional uploads and a duplicate-name search control. Static qualification was tightened and a regression test added. Superseded local prototypes were kept in work/; final deliverables use the corrected matcher. No historical evidence or live business record was rewritten.

Validation: see test-summary.json for final counts and exact commands, demo-browser-tests.json, fresh evidence, Hermes probe/smoke and local-installation.json. Existing database connection ResourceWarnings and two skipped tests are retained in logs. No model-backed success is claimed.

Rollback: the preflight backup and local install backup are referenced in change-manifest.json. The supplied rollback-local.py checks installed hashes before touching any file; it restores only this run's changed paths and leaves the database and subsequent user edits alone. The binary repair patch is also reversibly tested in a disposable checkout. A new isolated venv can remain after rollback without changing global packages.

Evidence is in the sibling outputs/evidence directory. All times in evidence are UTC; this run occurred 13 September 2026 in Pacific/Auckland.
