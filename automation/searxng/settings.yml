use_default_settings: true

general:
  debug: false
  instance_name: "WEBSITE-AUDITOR Local Search"
  enable_metrics: false

search:
  safe_search: 1
  formats:
    - json

server:
  # Overridden at runtime by SEARXNG_SECRET.
  secret_key: "replace-via-env"
  limiter: false
  public_instance: false
  image_proxy: false
