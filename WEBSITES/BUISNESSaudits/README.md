# WEBSITES/BUISNESSaudits

Canonical project namespace. Source stays in the existing repository layout.
Current reports are in `../../reports/`; new isolated validation and pilot runs
go in `runs/`. No live CRM mutation or external action is authorized by an output.
